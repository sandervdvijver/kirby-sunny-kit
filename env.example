# Remote server configuration
REMOTE_SERVER=user@yourserver.com
REMOTE_PATH=/path/to/your/kirby/site

# Optional: SSH port (default is 22)
# SSH_PORT=22

# Optional: SSH key path (if not using default)
# SSH_KEY_PATH=~/.ssh/id_rsa

# Optional: Remote backup directory
# REMOTE_BACKUP_DIR=/path/to/backups

# Optional: Slack webhook for notifications
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK 